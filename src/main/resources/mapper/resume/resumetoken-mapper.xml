<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ict.finalproject.resume.mapper.ResumeTokenMapper">

    <insert id="insertResumeToken" parameterType="ResumeTokenVO">
        INSERT INTO token_usage_log (
        t_m_idx,
        t_r_idx,
        t_total,
        t_purpose
        ) VALUES (
        #{mIdx},
        #{rIdx},
        #{tTotal},
        #{tPurpose}
        )
    </insert>

    <update id="updateResumeToken">
        UPDATE token_usage_log
        SET t_total = t_total + #{tTotal}
        WHERE t_r_idx = #{rIdx}
        AND t_purpose = 'RESUME'
    </update>

</mapper>